---
all:
  children:
    docker_containers:
      hosts:
        xvfb-on-package-image:
          ansible_connection: docker
          ansible_host: xvfb-on-package-image
          ansible_python_interpreter: /usr/bin/python3
