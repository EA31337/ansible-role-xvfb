---
# Vars file for xvfb role.
xvfb_nixos_install_pkgs:
  - name: python3.12-supervisor
    package: nixpkgs.python3Packages.supervisor
  # Note: xorg-server package provides: X Xephyr Xnest Xorg Xvfb gtf
  - name: xorg-server
    package: nixpkgs.xorg.xorgserver
xvfb_role_fact:
  xvfb_package_installed: false
